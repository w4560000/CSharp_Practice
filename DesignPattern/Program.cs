using CommonClassLibary;

namespace DesignPattern
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            SwitchExecute.Observer.ToString().EnterMethod(typeof(Program).Namespace);
        }
    }
}